import React from "react";

const IconService = ({ icon }) => {
    return (
            <>
                <img src={icon} className="w-full h-full bg-cover" alt="" />
            </>
    );
};

export default IconService;
